package com.sagittarius.bean.query;

public enum NumericFilterType {
    /**
     *  EQ means equal, NEQ means not equal,
     *  GT means greater than, LT means less than
     *  GTE means greater than or equal, LTE means less than or equal
     */
    EQ, NEQ, GT, LT, GTE, LTE
}
